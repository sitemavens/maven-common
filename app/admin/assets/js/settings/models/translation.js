define([],function(){
	
	var Translation = Backbone.Model.extend({
		action: 'translationEntryPoint',
		defaults: {
			
		},
		
		// Constructor
		initialize: function() {

		},

		// Any time a Model attribute is set, this method is called
		validate: function(attrs) {

		}
	
	});
	
	return Translation;

});
